### 在js中 所有的对象都是函数 所有的函数也可以称为"对象"

### js中的内置对象 

#### Date对象 创建一个实例对象 var date = new Date();
##### Date对象的方法
+ date.getFullYear(); 获取计算机当前时间的 年
+ date.getMonth()+1;  获取计算机当前时间的 月 因getMonth范围是0-11所以需要+1
+ date.getDate(); 获取计算机当前时间的	   日
+ date.getHours(); 获取计算机当前时间的  小时
+ date.getMinutes(); 获取计算机当前时间的分钟
+ date.getSeconds(); 获取计算机当前时间的秒钟
+ date.getMilliseconds(); 获取计算机当前时间的毫秒 1s = 1000 ms
+ date.getDay();  获取计算机当前时间的星期 
+ date.getTime(); 距离 1970年1月1日 0 时0 分  的 毫秒数

#### Math对象 Math对象不需要new 
##### Math对象的方法
+ Math.ceil(数字) 向上取整(数字如果有小数且小数点后数字大于0 向前进一位) 注意如果数字是负数 例如 -2.1 向上取整 == -2
+ Math.floor(数字) 地板函数  向下取整(去除小数点后的数) 注意如果数字是负数 例如-2.1 向下取整 == -3
+ Math.max(2,3,5,7 (多个数字) )  找出最大数
+ Math.min(2,3,5,7 (多个数字) )  找出最小数
+ Math.random()  随机数 Math.random() * 有多少个数 + 从哪个数开始
+ Math.PI  π 


### 对象类型是不能通过网络进行传输,只能是文本形式

### 对象 对象拥有:属性(特征),方法(行为/动态) var obj = {属性:'属性值'... 方法名:function(){ }}
#### 内置对象  Math Date 
#### 宿主对象 由JS的运行环境提供的对象，目前来讲主要指由浏览器提供的对象.比如BOM  DOM
#### 开发者自己定义的对象 
#### 创建对象
+ 使用new关键词创建一个实例对象
#### 对象名.属性名 = '属性值'; 为对象添加属性
#### 对象名.方法名:function(){ }   为对象添加方法
#### 自定义构造函数 中this 是一个指针 指向new出来的实例对象,如果没有new一个实例对象,那么this指向调用了这个构造函数的调用者.  this的指向在调用前是不确定的
#### 对象的遍历   for(var key in obj){ key == 属性名 : obj[key] == 属性值 }


### JSON 对象  JSON对象的属性有引号包裹  var jsonObj = {'属性':'属性值' ... }
+ 网络传输数据 需要将JSON对象转换成字符串  
+ 获取网络传输的信息后 需要将JSON字符串转换成JSON对象才能获取JSON对象的属性值
+ JSON.stringify(jsonObj) 将JSON对象装换成JSON字符串 不影响原对象
+ JSON.parse(jsonObj)  将JSON字符串装换成JSON对象 该方法也可以将普通对象转换成JSON对象 不影响原字符串
+ jsonObj.属性名  获取该属性的值 
+ 对象名.属性名 如果属性是一个变量 必须使用对象名[变量]形式取值

### 对象和json类型的对象都可以通过 JSON.stringify() 转换为字符串
+ 但是如果希望字符串转换json对象 必须用"" 例如 var str = "{属性:值}"

### 递归  自己调用自己  递归必须要有跳出条件 


### 三元运算  判断条件?为真的操作:为假的操作

### a++;先参与运算 再执行++   ++a;先执行++  再参与运算

### break 跳出当前for循环不再循环  
### continue  跳出当前循环进入下一次循环























